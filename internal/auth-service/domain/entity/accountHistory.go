package entity

/* import "time"

type AccountAction string

const (
	AccountActionSignup AccountAction = "signup"
	AccountActionLogin  AccountAction = "login"
	AccountActionLogout AccountAction = "logout"
)

type AccountHistory struct {
	ID        int64
	UserID    UserID
	Action    AccountAction
	Metadata  map[string]string
	CreatedAt time.Time
} */
